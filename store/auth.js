// States
export const state = () => ({
  isAuth: false,
})

// mutations
export const mutations = {}

// actions
export const actions = {}

// Getters
export const getters = {
  isAuth: (state) => state.isAuth,
}
